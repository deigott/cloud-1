---
- name: Build mariadb container
  community.docker.docker_compose:
    project_src: '{{ webapp_path }}'
    project_name: webapp
    services:
      - mariadb
    build: false
    stopped: false
    state: present